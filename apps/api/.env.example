NODE_ENV=development
PORT=3001

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ffm?schema=public"

JWT_SECRET="change-me-to-a-long-random-string"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="change-me-to-another-long-random-string"
JWT_REFRESH_EXPIRES_IN="7d"

CORS_ORIGIN="http://localhost:3000"

# Public base URL returned in upload file_url (use http://api:3001 in Docker if web calls API by service name)
# API_PUBLIC_URL="http://localhost:3001"

# Optional AI (service-type step generator, etc.)
# OPENAI_API_KEY=""
# OPENAI_MODEL="gpt-4o-mini"

# Optional: folder with Base44 *_export.csv files (defaults to ../../../database next to monorepo root)
# SEED_CSV_DIR="C:\\Project_new\\iCrewForce\\iCrewForce\\database"
